# 项目笔记

## 1、类的设计

### 患者

```C++
class Patient{
  string id[MAX_ID_LEN];
  string name[MAX_NAME_LEN];
  int age;
  string gender[10];
  string phone[15];
  string id_card[20];
  long long balance; //账户余额
  string pin[7]; //6位登陆密码
}
```

### 医生

```C++
class Doctor{
  string id[MAX_ID_LEN];
  string name[MAX_NAME_LEN];
  string dept_id[MAX_ID_LEN];
  string spcialty[MAX_SPECIALTY_LEN]; //擅长领域
  string account[MAX_ACCOUNT_LEN];//账户
  string password[MAX_PWD_LEN];
}
```

### 科室

```C++
class Department{
  string id[MAX_ID_LEN];
  string name[MAX_NAME_LEN];
  int doctor_count;
}
```

### 药品

```C++
class Drug{
  string id[MAX_ID_LEN];
  string general_name[MAX_NAME_LEN];//通用名
  string trade_name[MAX_NAME_LEN];//商品名
  string alias[MAX_NAME_LEN];//别名
  float price;
  int stock;
  int warning_stock;
  vector<string>;//关键修改，修正原有一个药品绑定一个科室的错误
}
## c### dan zui`#define
``C++
```
# 床位

/#######/lflicoC++cocoi###/C++




